info:
  name: delete /brands/{id}
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8080/api/brands/4
  headers:
    - name: X-XSRF-TOKEN
      value: "{{xsrf_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
