info:
  name: delete /brands/{id}
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/api/brands/4
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
