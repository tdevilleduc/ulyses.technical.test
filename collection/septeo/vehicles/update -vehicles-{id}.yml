info:
  name: update /vehicles/{id}
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:8080/api/vehicles/10
  headers:
    - name: X-XSRF-TOKEN
      value: "{{xsrf_token}}"
  body:
    type: json
    data: |-
      {
        "brand": {
          "id": 2
        },
        "model": "Megane 3",
        "year": "2025",
        "color": "Purple"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
