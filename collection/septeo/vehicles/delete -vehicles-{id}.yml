info:
  name: delete /vehicles/{id}
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8080/api/vehicles/10
  headers:
    - name: X-XSRF-TOKEN
      value: "{{xsrf_token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
