info:
  name: create /vehicles
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/vehicles
  headers:
    - name: X-XSRF-TOKEN
      value: "{{xsrf_token}}"
  body:
    type: json
    data: |-
      {
        "brand": {
          "id": 1
        },
        "model": "Megane 2",
        "year": "2026",
        "color": "Green"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
